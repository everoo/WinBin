import 'package:flutter/material.dart';

class ReadMusic extends StatefulWidget {
  final String docRef;

  const ReadMusic({Key key, this.docRef}) : super(key: key);
  @override
  _ReadMusicState createState() => _ReadMusicState();
}

class _ReadMusicState extends State<ReadMusic> {
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}